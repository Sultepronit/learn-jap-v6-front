Add to the server & the client forced update if the client update is too stale

Do something with creation of half card

Add alt mode to the words session! Ignore bad reading if alt or several readings!