add {}[]() cases to search algorithm
implement search for new words!

Ignore bad reading if alt or several readings?..

Add to the server & the client forced update if the client update is too stale

forbid update after delete?